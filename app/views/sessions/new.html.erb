<% provide(:title, "User Login")
<div>
	<%= form_for(:session, url: login_path, method: 'post') do |f| %>

		<%= f.label :email %>
		<%= f.email_field :email %>

		<%= f.label :password %>
		<%= f.password_field :password %>

		<%= f.submit "Log in" %>
	<% end %>
</div>